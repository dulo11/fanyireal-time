# 浮译 0.7.2.2 正式修复版

本版继续修复 0.7.2 系列，重点处理“检查更新”在部分移动网络、漫游或 VPN 环境下长期卡住/直接失败的问题。

- 更新检测不再只依赖 `api.github.com`。
- 现在优先访问普通 `github.com` 的 Latest Release 页面，并通过最终 Release 地址解析稳定版版本号。
- 如果 GitHub 网页检查失败，再自动回退到 GitHub REST API。
- 普通 GitHub 能打开、但 `api.github.com` 被当前网络阻断时，仍可以正常检查更新。
- 连接和读取超时缩短，避免一次检查长时间卡住。
- 两条线路都失败时，会同时显示“发布页”和“API”的失败原因，并明确提示检查网络/VPN/漫游。
- 下载仍使用 GitHub Latest Release 的 `FloatingTranslator-latest.apk`，不改变原有固定签名和升级方式。
- 保留 0.7.2.1 的大模型下载缓存保护：完整模型包只有安装成功后才删除，解压失败可直接重新安装，不会重复下载几 GB。
- 保留 0.7.2 的自动多语言实时翻译、全局翻译、连续面对面对话、Qwen3/Whisper 自动 ASR 路由等全部功能。

这是 v0.7.2.2 正式热修复版本，继续使用同一固定签名，可直接覆盖安装 0.7.2 / 0.7.2.1。
