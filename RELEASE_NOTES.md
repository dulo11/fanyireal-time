# 浮译 0.7.2.4 正式修复版

修复全屏翻译只显示少量文字的多处原因：

- 默认无障碍文字与整屏截图 OCR 合并；即使已读到标题或几个按钮，也补识别图片和漏读文字。可在全屏设置中关闭补齐。
- 长文字不再超过 1000 字符整段丢弃；按请求限制拆分，超过 80 条或旧总字符上限也继续分批处理。
- 保留原文与结果对应关系；失败条目标明原因并继续处理后续内容，已完成部分逐步显示。失败结果不缓存，点全文中的重试可重新扫描。
- 只去除相同文字的重叠浮层，不因包含小标题或按钮而删除独有正文。
- 新增“全文 已处理/总数”入口，滚动查看识别到的全部原文、译文和失败原因。原位浮层保留紧凑预览，避免长段落画成整屏黑块。
- 滚动、换页后立即清除旧位置浮层；过期截图和翻译结果不再回填到新页面。忙碌期间记录后续扫描请求。
- 截图失败时保留已读取的无障碍文字并提示覆盖限制；OCR 与译文详情仅保存在当前服务内存中。

## 使用

升级后进入全屏翻译，确认“无障碍截图 OCR”和“整屏 OCR 补齐图片 / 漏读文字”开启；回到目标页面轻点译悬浮球。长正文点屏幕右下方“全文”查看，失败段可用“重新扫描 / 重试”再处理。需要滚动后自动翻译时开启全局自动翻译。

## 验证与限制

本版增加长文本完整性、超出旧条目与字符上限、失败批次继续、结果对应关系、长节点采集、父子框去重及滚动刷新回归测试。发布执行测试、Lint、签名构建及与旧版证书连续性校验。

尚未连接用户实体手机，不能把自动测试等同于 Telegram 真机覆盖率验证。OCR 使用已接入的 ML Kit 文字模型，不能识别所有文字体系；西里尔字母等未接入的文字仍依赖 App 提供的文字节点。禁止截图、隐藏文字或不支持的识别模型仍可能限制覆盖。全文面板显示本轮扫描快照；页面变动后需要重新扫描。
