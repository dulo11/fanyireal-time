# 浮译 0.7.2.1 正式修复版

本版针对 0.7.2 离线大模型下载完成后，解压/安装失败导致完整下载包被删除的问题进行专项修复。

- 下载完成后先把完整压缩包标记为“已完整下载待安装”，不会立即删除。
- 只有模型成功解压、检测到 ONNX 文件、完成目录切换并写入 `.ready` 后，才删除完整下载包。
- 解压或安装失败时保留完整下载包；再次点击会直接重新解压/安装，不重复下载几 GB 文件。
- 网络中断仍保留 `.part` 文件，继续支持断点续传。
- 模型中心现在区分“未下载 / 下载未完成 / 完整下载待安装 / 已安装”四种状态。
- 如果完整下载包已存在，下载按钮会变成“重新解压 / 安装”。
- 修复安装错误提示被“模型状态已刷新”立即覆盖的问题，失败原因会保留在页面上。
- 模型中心新增完整下载待安装缓存、未完成缓存和手机当前可用空间显示。
- “清理未完成下载缓存”调整为“清理下载 / 安装缓存”，用户可主动删除损坏或不再需要的缓存。
- 对“手机存储空间不足”增加更明确的提示；即使空间不足导致解压失败，完整下载包仍会保留。
- 保留 0.7.2 的自动多语言实时翻译、全局翻译、连续面对面对话、Qwen3/Whisper 自动 ASR 路由等全部功能。

这是 v0.7.2 的正式热修复版本，版本号 v0.7.2.1，继续使用同一固定签名，可直接覆盖安装 0.7.2。
