# 浮译 0.7.2-dev1 测试版

- 修复实时翻译、ROOT / Shizuku 通话翻译在自动语言模式下误反向的问题：普通翻译现在始终是“自动识别源语言 → 固定目标语言”。
- 只有“面对面连续双向对话”保留自动反向：对方语言 → 我的语言；我的语言 → 最近识别到的对方语言。
- 多语言 ASR 返回的语言码优先于文本语言判断；没有语言码时再使用 ML Kit Language ID 本地识别。
- 修复类似“土 / 東京”这类短汉字文本被误判为中文后自动反向的问题：普通实时翻译会优先使用用户设置的备用原语言。
- 如果识别结果本来就是目标语言，普通实时/通话翻译直接保留原文，不再自动翻到其他语言。
- 全局无障碍翻译在没有 Azure 时也改成离线自动识别：每个文字块先用本机 Language ID 判断语言，再动态选择 ML Kit 翻译方向；一个页面混合日语、英语、越南语时不再必须手动切原语言。
- 修正 Hebrew / Indonesian / Filipino 等语言别名判断，并加强翻译请求关闭/并发清理。
- 保留 0.7.1-dev2 的 Telegram/网页长消息优化、智能 OCR、连续面对面对话、会话记录等功能。
- 不改 API Key 本地明文保存方式，不做 APK 瘦身。

## 重点测试

1. 实时翻译设置“备用原语言=日语、翻译为=中文、识别语言模式=自动多语言”，分别播放日语、英语、中文，确认不会再出现 `zh→ja` 之类自动反向。
2. 使用 Vosk 日语时测试短词/单字，例如“土”，应按日语备用源语言处理，不应被错误反向。
3. 使用 Whisper Small / Medium、Qwen3-ASR、SenseVoice 等多语言模型，确认 ASR 返回语言码后会自动切换源语言，但目标始终保持中文。
4. 关闭/不配置 Azure 后测试全局网页或 Telegram，确认离线 ML Kit 可以自动判断不同文字块的源语言并统一翻译到中文。
5. 面对面连续模式仍应自动双向，不受普通实时翻译“固定目标语言”规则影响。

这是 0.7.2 的第一版测试构建，0.7.0 稳定回滚分支和此前 Release 保留不动。
