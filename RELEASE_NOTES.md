# 浮译 0.7.2.3 正式修复版

针对连续对话中“说英语却显示中文原文并翻成日语”的问题，分开处理语音识别与翻译方向：

- 新增三种语言模式：自动识别双向、限定双方语言自动双向、固定输入语言→我的语言。
- 固定输入会传入真正的 ASR 单语言设置，同时固定翻译方向。英语→中文：输入语言选英语，我的语言选中文。
- Qwen3/Omnilingual 当前接入接口没有强制输入语言参数，固定输入模式会明确要求选择支持语言约束的 Whisper、SenseVoice、Vosk 或系统识别；不会伪装成已锁定语言。
- 自动模式复核 ASR 标签与文字语言置信度；冲突或缺少可靠证据时显示待确认，不再直接猜备用日语方向。
- 统一 English/Japanese、区域标签及 ASR 控制标签的处理，去掉“拉丁字母自动当英语”的规则。
- 限定双方语言时拒绝第三种语言，不自动改写对方语言；系统语音服务支持时设置候选语言范围。
- 禁止把 Vosk、日语专用模型当成连续自动双向识别器；统一模型选择，界面选中后不再内部换模型。
- 多语言自动识别不再使用上一句音频做二次上下文重解码，减少换语言时上一句的干扰。
- 每句显示识别原文、ASR 语言标签与最终翻译方向；失败句保留在对话记录中供核对。
- 连续对话朗读改为独立开关，默认关闭，减少手机朗读被麦克风再次录入；单句朗读设置保留。
- 系统识别重启后忽略旧一轮的回调，保留一次开启持续监听能力。
- 保留 0.7.2.2 的更新网络双线路、全屏翻译和固定签名升级能力。

## 建议操作

如果当前主要听英语：对话→语言模式选择“固定输入语言→我的语言”；输入选择英语，我的语言选择中文，ASR 选择自动或已下载的 Whisper/SenseVoice，再开启连续麦克风。

若需要中英双向：我的语言选择中文，对方语言选择英语，模式选择“限定双方语言·自动双向”；搭配支持中英的多语言模型。自动识别模式可根据新检测到的外语更新回复目标。

固定输入用于单向连续收音；自动/限定双向用于双方连续对话。模式与语言设置在停止收音后修改。系统服务可能忽略自动切换参数，固定模式通常更可控。

## 验证范围

发布构建执行单元回归、Android Lint、签名检查与旧版本证书连续性校验。没有连接实体手机或收到原始问题录音，不能保证 ASR 对所有短句的声学识别准确；完全听错的原文无法仅靠翻译路由恢复。
